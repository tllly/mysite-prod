# 外贸独立站 - 项目运营与内容管理说明书

本手册旨在帮助网站管理员、运营人员和技术人员快速上手并有效管理本外贸独立站项目。

---

## 一、 网站运营说明书 (Website Operations Manual)

### 1. 管理后台访问
*   **地址**: `你的域名/admin/`
*   **账号**: `admin`
*   **密码**: (请在交接时由技术人员告知)

### 2. 全局设置 (Site Settings)
在后台导航栏的 `Settings` (设置) 菜单下，包含以下核心运营配置：

#### A. 基础联系设置 (Navigation Settings)
*   **WhatsApp**: 填写带国家代码的号码（如 `86138...`）。填写后，全站右下角会自动出现悬浮聊天按钮。
*   **社交媒体**: 填写 LinkedIn, Facebook 的完整 URL。
*   **Email**: 网站显示的官方联系邮箱。

#### B. 追踪与 SEO 代码 (Tracking Settings)
*   **GA4 ID**: 填入 `G-XXXXX` 格式的代码，系统会自动集成 Google Analytics。
*   **GTM ID**: 填入 `GTM-XXXXX` 格式的代码。
*   **自定义脚本**: 允许在 `<head>` 或 `<body>` 中插入第三方客服插件或统计代码。**注意：插入代码前请务必确认代码来源安全。**

### 3. 多语言运营流程
本站基于 `Wagtail Localize` 实现了高度自动化的多语言管理：

**关键前置步骤 (非常重要)：**
*   **激活语言**: 我已在数据库中预设了英语、西班牙语、法语、阿拉伯语和中文。如果以后需要增加新语言，请在后台 `Settings -> Locales` 中添加。
*   **翻译页面**: 只有当某个页面被“翻译”并发布后，多语言切换器才会精准跳转。如果页面尚未翻译，切换器将默认尝试跳转到该语言的首页。

**翻译操作步骤：**
1.  **发布主语言内容**: 始终先在“英语 (English)”下创建和发布内容。
2.  **提交翻译**: 在页面编辑界面的右上角点击 `Translate` (翻译)，选择目标语言。
3.  **内容同步**: 修改主页面后，系统会提示同步更新翻译版本。

### 4. 询盘 (Inquiry) 管理
*   **线索接收**: 客户填写的询盘会发送至 `Inquiry Page` 中设置的 `To Address` 邮箱。
*   **后台查看**: 也可以在 `Forms` (表单) 模块下点击 `Inquiry Page` 查看所有历史询盘记录，并支持导出为 Excel。

---

## 二、 产品内容管理说明书 (Product Management Manual)

产品是外贸站的核心，必须确保信息的准确性与专业感。

### 1. 创建产品 (Product Page)
在 `Products` 页面下创建子页面。

#### 核心 B2B 字段说明：
*   **SKU / Item No.**: 必填，用于准确识别产品。
*   **Price**: 显示价格（可选）。若不填，前端将只显示“询价”按钮。
*   **MOQ (最小起订量)**: 如 `500 Pieces` 或 `1 Ton`。这是采购商筛选供应商的关键参数。
*   **Lead Time (交货期)**: 如 `15-30 Days`。
*   **Product Video URL**: 仅支持 YouTube 或 Vimeo 的完整链接。系统会自动在详情页嵌入视频播放器。

### 2. 图片管理规范
*   **主图**: 建议使用 **800x800 像素** 以上的正方形图片，白底最佳。
*   **Gallery (图集)**: 至少上传 3-5 张细节图（包装图、工厂实拍、不同角度图）。
*   **ALT 标签**: 上传图片时请务必填写 `Title` 或 `Description` 字段，系统会自动将其转为图片的 `ALT` 标签，这对 Google 图片搜索排名至关重要。

### 3. SEO 最佳实践
*   **Page Title**: 不要只写产品名，应包含核心关键词，如 `[产品名] Supplier from China`。
*   **Search Description**: 简述产品核心卖点，吸引搜索用户点击。
*   **JSON-LD**: 系统已自动为每个产品页生成结构化数据，无需手动配置。

---

## 三、 维护与技术注意事项 (Maintenance Notes)

### 1. 静态文件与存储
*   **图片处理**: 网站会自动对上传的图片进行裁剪和优化，无需手动压缩。
*   **环境变更**: 若从测试环境迁移至生产环境，需在 `.env` 中更新 `DJANGO_ALLOWED_HOSTS` 和 `DATABASE_URL`。

### 2. 常见问题排查
*   **页面 404**: 检查该页面是否处于 `Live` (发布) 状态，或检查该语言的 Locale 是否已在后台 `Locales` 中激活。
*   **邮件发不出**: 检查 `mysite/settings/production.py` 中的 `EMAIL_HOST` 等配置是否正确。

---
**版本**: 1.0  
**生成日期**: 2026年5月14日
